{
  "name": "scanbell-monorepo",
  "version": "2.0.0",
  "description": "ScanBell - Smart Doorbell with Facial Recognition (2026 Edition)",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "supabase:start": "supabase start",
    "supabase:stop": "supabase stop",
    "supabase:push": "supabase db push"
  },
  "devDependencies": {
    "@turbo/gen": "^2.3.0",
    "@types/speakeasy": "^2.0.10",
    "prisma": "^6.0.0",
    "turbo": "^2.9.5",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "speakeasy": "^2.0.0"
  }
}
